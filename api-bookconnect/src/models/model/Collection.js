export default class Collection {

    constructor(id, user, book, state, rating, review) {
      this.id = id;
      this.user = user;
      this.book = book;
      this.state = state;
      this.rating = rating;
      this.review = review;
    }
  
  }