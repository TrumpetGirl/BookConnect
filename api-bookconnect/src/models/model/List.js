// Modelo de la clase de lista
export default class List {

    constructor(id, name, description, username, userId) {
      this.id = id;
      this.name = name;
      this.description = description;
      this.username = username;
      this.userId = userId;
    }
  }