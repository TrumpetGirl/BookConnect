// Modelo de la clase autor
export default class Author {

    constructor(id, name, birth_date, nationality, image_path) {
      this.id = id;
      this.name = name;
      this.birth_date = birth_date;
      this.nationality = nationality;
      this.image_path = image_path;
    }
  
  }