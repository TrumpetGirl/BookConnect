// Modelo de la clase autor
export default class Author {

    constructor(id, name, birthdayDate, nationality) {
      this.id = id;
      this.name = name;
      this.birthdayDate = birthdayDate;
      this.nationality = nationality;
    }
  
  }