export default class SearchElement {

    constructor(id, description, image_path, type) {
      this.id = id;
      this.description = description;
      this.image_path = image_path;
      this.type = type;
    }
  
  }